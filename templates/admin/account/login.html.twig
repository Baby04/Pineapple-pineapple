<!DOCTYPE html>
<html lang="en">
	<head>
		<meta charset="UTF-8">
		<meta name="viewport" content="width=device-width, initial-scale=1.0">
		<meta http-equiv="X-UA-Compatible" content="ie=edge">
		<title>Admin Connection</title>
		<link rel="stylesheet" href="https://bootswatch.com/4/cosmo/bootstrap.min.css">
		<script src="https://kit.fontawesome.com/bed2bfd205.js" crossorigin="anonymous"></script>
		<link rel="icon" href="{{ asset ('image/logo.png')}}" type="image/x-icon">


		<link rel="stylesheet" href="{{ asset('Css/bootstrap.min.css') }}">

		<link rel="stylesheet" href="{{ asset('cssa/all.css')}}">

		<style>
			body {
				display: flex;
				justify-content: center;
				height: 100vh;
			}

			.card {
				margin: 10%;
			}
		</style>
	</head>
	<body>
		<div class="col-md-6">
			<div class="card bg-light">
				<div class="card-header">
					<h4 class="title">
						<i class="fas fa-lock"></i>
						Admin Connection
					</h4>
				</div>
				<div class="card-body">
					{% if hasError %}
						<div class="alert alert-danger">
							Your credentials are incorrect, are your trying to break in like a thief!!!
						</div>
					{% endif %}
					<form action="{{ path('admin_account_login') }}" method="post">

						<div class="form-group">
							<label for="_username">Email</label><input type="text" class="form-control" placeholder="Email Addresse" name="_username" value="{{username}}"></div>
						<div class="form-group">
							<label for="_password">Password</label><input type="password" class="form-control" placeholder="Password" name="_password"></div>
						<div class="form-group">
							<button type="submit" class="btn btn-primary">
								<i class="fas fa-lock-open"></i>
								Connect
							</button>
							<a href="{{ path('welcome') }}" class="btn btn-link">
								<i class="fas fa-arrow-circle-left"></i>
								Return to Homepage
							</a>
						</div>
					</form>
				</div>
			</div>
		</div>

	</body>
</html>
