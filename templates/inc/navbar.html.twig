<nav class="navbar  navbar-expand-lg  navbar-dark bg-dark fixed-top">


	<a href="/" class="navbar-brand"><img src="{{ asset ('image/P logo.png')}}" width="80" height="50" class="d-inline-block align-top" alt=""></a>

	<button class="navbar-toggler collapsed" type="button" data-toggle="collapse" data-target="#navbarResponsive" aria-controls="navbarResponsive" aria-expanded="false" aria-label="Toggle navigation">
		<span class="navbar-toggler-icon"></span>
	</button>


	<div class="collapse navbar-collapse" id="navbarResponsive">

		<ul class="navbar-nav mr-auto">
			<li class="nav-item">
				<a href="/" class="nav-link">
					<i class="fas fa-home"></i>
					Home</a>
			</li>
			<li class="nav-item">
				<a href="{{path('welcome')}}" class="nav-link">
					<i class="fas fa-store"></i>
					Buy
				</a>
			</li>
			<li class="nav-item">
				<a href="{{path('home')}}" class="nav-link">About
				</a>
			</li>
		</ul>

		<ul class="navbar-nav ml-auto">
			{% if app.user %}
				{#<li class="nav-item"><a href="{{ path('account_logout')}}" class="nav-link"><i class="fas fa-sign-out-alt"></i>Sign out</a></li>#}
				<li class="nav-item dropdown">
					<a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
						<i class="fas fa-user"></i>
						<strong>{{app.user.firstName}}
							{{app.user.lastName}}</strong>
					</a>
					<div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
						<a href="{{ path('user_show', {'id': app.user.id})}}" class="dropdown-item">
							<i class="fas fa-user-circle">
								My account</i>
						</a>

						<a href="{{ path('cart')}}" class="dropdown-item">
							<i class="fas fa-shopping-cart">
								My cart</i>
						</a>
						<a href="{{ path('account_orders')}}" class="dropdown-item">
							<i class="fas fa-box">
								My orders</i>
						</a>

						<div class="dropdown-divider"></div>
						<a href="{{ path('account_logout')}}" class="dropdown-item">
							<i class="fas fa-sign-out-alt">
								Sign out</i>
						</a>
					</div>
				</li>
			{% else %}
				<li class="nav-item">
					<a href="{{ path('account_register') }}" class="nav-link">
						Create an account
					</a>
				</li>

				<li class="nav-item">
					<a href="{{ path('account_login') }}" class="nav-link">
						<i class="fa fa-sign-in-alt"></i>
						Sign In</li>
				</li>
			</a>


		{% endif %}
	</ul>


</div>
<br></nav>
